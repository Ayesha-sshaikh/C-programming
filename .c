#include<stdio.h>
int main(){
    int l,area;
    //Area of square
    printf("enter length of square l: ");
    scanf("%d", &l);
    area= l*l;
    printf("area of square is: %d",area);
    return 0;

}

